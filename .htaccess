<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Если URL уже начинается с /public — не трогаем
    RewriteCond %{REQUEST_URI} ^/public
    RewriteRule ^ - [L]

    # Всё остальное — в public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>